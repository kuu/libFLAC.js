{
  "defaultTarget": "all",
  "properties": {
    "sourceDir": "src",
    "buildDir": "build",
    "binDir": "bin"
  },
  "plugins": {},
  "targets": {
    "all": {
      "builder": "js",
      "outputs": "${binDir}/libFLAC.js",
      "resources": [
        {
          "type": "files",
          "root": "${sourceDir}",
          "include": "\\.js$"
        }
      ]
    }
  }
}
